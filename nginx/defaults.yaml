{% load_yaml as rawmap %}
Debian:
  pkgs: {}
  service: {}
  user: www-data
  vhosts:
    dir: /etc/nginx/sites-available
  ssl:
    dir: /etc/nginx
RedHat:
  pkgs: {}
  service: {}
  user: nginx
  vhosts:
    dir: /etc/nginx/sites-available
  ssl:
    dir: /etc/nginx
{% endload %}
